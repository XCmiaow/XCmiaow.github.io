---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="course-block course-block--concept" aria-label={`核心概念：${title}`}>
  <p class="course-block__label">核心概念</p>
  <h2>{title}</h2>
  <div class="course-block__body"><slot /></div>
</section>
