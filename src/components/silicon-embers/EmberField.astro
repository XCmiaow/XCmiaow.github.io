<div class="ember-stage" aria-hidden="true">
  <div class="black-hole-scene">
    <div class="ambient-buffer"></div>
    <div class="gravity-halo"></div>
    <div class="depth-well"></div>
    <div class="warp-sheet warp-back"></div>
    <div class="lensing-shell shell-outer"></div>
    <div class="lensing-shell shell-inner"></div>
    <div class="shadow-sheet"></div>
    <div class="accretion-disc disc-back"></div>
    <div class="photon-ring"></div>
    <div class="event-horizon"></div>
    <div class="accretion-disc disc-front"></div>
    <div class="warp-sheet warp-front"></div>
    <div class="near-side-shadow"></div>
    <div class="event-shadow"></div>
  </div>
  <canvas data-ember-canvas></canvas>
</div>

<style>
  .ember-stage {
    --hole-x: 65%;
    --hole-y: 50%;
    --hole-size: min(48vw, 660px);

    position: absolute;
    inset: 0 max(-8vw, -118px) 0 34%;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
    mask-image: linear-gradient(90deg, transparent 0%, #000 18%, #000 86%, transparent 100%);
  }

  .ember-stage::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 5;
    background: linear-gradient(90deg, rgba(6, 5, 3, 0.96) 0%, rgba(6, 5, 3, 0.58) 42%, transparent 78%);
    opacity: 0;
    pointer-events: none;
  }

  canvas,
  .black-hole-scene,
  .ambient-buffer,
  .gravity-halo,
  .depth-well,
  .warp-sheet,
  .lensing-shell,
  .shadow-sheet,
  .accretion-disc,
  .photon-ring,
  .event-horizon,
  .near-side-shadow,
  .event-shadow {
    position: absolute;
  }

  canvas {
    inset: 0;
    z-index: 4;
  }

  .black-hole-scene {
    top: var(--hole-y);
    left: var(--hole-x);
    z-index: 2;
    width: var(--hole-size);
    aspect-ratio: 1;
    transform: translate(-50%, -50%);
    transform-style: preserve-3d;
    opacity: 1;
  }

  .black-hole-scene::before,
  .black-hole-scene::after {
    content: '';
    position: absolute;
    left: 50%;
    border-radius: 50%;
    pointer-events: none;
  }

  .black-hole-scene::before {
    top: 49%;
    z-index: 2;
    width: 168%;
    height: 55%;
    background:
      radial-gradient(
        ellipse at 51% 50%,
        transparent 0 24%,
        rgba(178, 132, 78, 0.18) 42%,
        rgba(72, 96, 88, 0.06) 58%,
        transparent 76%
      ),
      linear-gradient(
        90deg,
        transparent 0%,
        rgba(116, 94, 67, 0.12) 24%,
        rgba(214, 158, 88, 0.16) 51%,
        rgba(73, 96, 88, 0.06) 72%,
        transparent 100%
      );
    filter: blur(2.6px);
    mix-blend-mode: screen;
    transform: translate(-50%, -50%) rotate(-10deg) scaleY(0.62);
    opacity: 0.9;
    mask-image: linear-gradient(
      90deg,
      transparent 0%,
      rgba(0, 0, 0, 0.68) 14%,
      #000 28%,
      #000 72%,
      rgba(0, 0, 0, 0.68) 86%,
      transparent 100%
    );
  }

  .black-hole-scene::after {
    top: 53%;
    z-index: 8;
    width: 124%;
    height: 21%;
    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(218, 158, 88, 0.24) 30%,
        rgba(238, 190, 112, 0.28) 49%,
        rgba(34, 23, 16, 0.24) 64%,
        transparent
      ),
      linear-gradient(180deg, transparent 0 43%, rgba(0, 0, 0, 0.36) 69%, transparent 100%);
    filter: blur(1.8px);
    mix-blend-mode: screen;
    transform: translate(-50%, -50%) rotate(-10deg) scaleY(0.56);
    opacity: 0.86;
    mask-image: linear-gradient(
      90deg,
      transparent 0%,
      rgba(0, 0, 0, 0.7) 18%,
      #000 32%,
      #000 70%,
      rgba(0, 0, 0, 0.7) 84%,
      transparent 100%
    );
  }

  .ambient-buffer {
    inset: -28%;
    z-index: -1;
    border-radius: 50%;
    background:
      radial-gradient(
        ellipse at 52% 51%,
        rgba(238, 179, 103, 0.11) 0 12%,
        rgba(128, 81, 43, 0.1) 31%,
        rgba(31, 23, 17, 0.08) 54%,
        transparent 76%
      ),
      radial-gradient(ellipse at 36% 54%, rgba(119, 145, 134, 0.055), transparent 52%),
      radial-gradient(ellipse at 68% 45%, rgba(214, 142, 76, 0.08), transparent 60%);
    filter: blur(34px);
    mix-blend-mode: screen;
    opacity: 0.82;
    transform: rotate(-10deg) scale(1.08, 0.92);
  }

  .gravity-halo {
    inset: 1%;
    z-index: 0;
    border-radius: 50%;
    background:
      radial-gradient(
        ellipse at 52% 50%,
        rgba(0, 0, 0, 0.16) 0 27%,
        rgba(105, 65, 34, 0.23) 47%,
        rgba(35, 25, 18, 0.1) 64%,
        transparent 80%
      ),
      radial-gradient(circle at 41% 30%, rgba(120, 155, 145, 0.085), transparent 42%),
      conic-gradient(
        from 218deg,
        rgba(0, 0, 0, 0.14),
        rgba(145, 92, 47, 0.2),
        transparent 34% 58%,
        rgba(70, 96, 88, 0.07),
        transparent 78% 100%
      );
    filter: blur(5px);
    opacity: 0.94;
    animation: gravity-turn 72s linear infinite;
  }

  .depth-well {
    inset: -10%;
    z-index: 1;
    border-radius: 50%;
    background:
      radial-gradient(ellipse at 51% 51%, rgba(0, 0, 0, 0.58) 0 23%, rgba(0, 0, 0, 0.26) 42%, transparent 68%),
      radial-gradient(ellipse at 58% 58%, transparent 0 26%, rgba(125, 74, 38, 0.25) 51%, transparent 78%),
      conic-gradient(
        from 235deg,
        rgba(0, 0, 0, 0.34),
        rgba(105, 74, 46, 0.2),
        transparent 29%,
        rgba(0, 0, 0, 0.45) 55%,
        rgba(87, 113, 108, 0.06),
        rgba(0, 0, 0, 0.22)
      );
    filter: blur(16px);
    transform: rotate(-11deg) scaleY(0.56);
    opacity: 0.92;
    animation: well-breathe 18s ease-in-out infinite;
  }

  .warp-sheet {
    left: 50%;
    border-radius: 50%;
    mix-blend-mode: screen;
    pointer-events: none;
  }

  .warp-sheet::before {
    content: '';
    position: absolute;
    right: 4%;
    left: 4%;
    border-radius: inherit;
    pointer-events: none;
  }

  .warp-back {
    top: 49%;
    z-index: 3;
    width: 182%;
    height: 44%;
    background:
      radial-gradient(
        ellipse at 57% 51%,
        transparent 0 23%,
        rgba(214, 160, 94, 0.17) 42%,
        rgba(77, 100, 92, 0.065) 60%,
        transparent 80%
      ),
      linear-gradient(
        90deg,
        transparent 0%,
        rgba(79, 103, 95, 0.055) 24%,
        rgba(226, 166, 94, 0.13) 49%,
        rgba(40, 29, 21, 0.11) 66%,
        transparent 100%
      );
    filter: blur(5px);
    transform: translate(-50%, -61%) rotate(-12deg) skewX(-8deg) scaleY(0.5);
    opacity: 0.86;
    mask-image: radial-gradient(
      ellipse at 57% 51%,
      transparent 0 20%,
      rgba(0, 0, 0, 0.66) 30%,
      #000 44% 64%,
      rgba(0, 0, 0, 0.6) 75%,
      transparent 88%
    );
    animation: warp-back-breathe 24s ease-in-out infinite;
  }

  .warp-back::before {
    top: 47%;
    height: 9%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(222, 169, 100, 0.17) 42%,
      rgba(114, 140, 130, 0.075) 58%,
      transparent
    );
    filter: blur(2.2px);
  }

  .warp-front {
    top: 54%;
    z-index: 8;
    width: 140%;
    height: 24%;
    background:
      linear-gradient(
        90deg,
        transparent 0%,
        rgba(204, 141, 75, 0.11) 21%,
        rgba(242, 186, 108, 0.22) 46%,
        rgba(23, 14, 9, 0.18) 60%,
        rgba(78, 100, 91, 0.075) 74%,
        transparent 100%
      ),
      radial-gradient(ellipse at 50% 50%, transparent 0 34%, rgba(0, 0, 0, 0.19) 56%, transparent 80%);
    filter: blur(2px);
    transform: translate(-50%, -50%) rotate(-10deg) skewX(8deg) scaleY(0.48);
    opacity: 0.78;
    mask-image: linear-gradient(
      90deg,
      transparent 0%,
      rgba(0, 0, 0, 0.6) 15%,
      #000 31%,
      #000 72%,
      rgba(0, 0, 0, 0.64) 86%,
      transparent 100%
    );
    animation: warp-front-breathe 18s ease-in-out infinite;
  }

  .warp-front::before {
    top: 43%;
    height: 12%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(244, 195, 118, 0.15) 38%,
      rgba(0, 0, 0, 0.28) 58%,
      transparent
    );
    filter: blur(1.5px);
  }

  .lensing-shell {
    top: 50%;
    left: 50%;
    border-radius: 50%;
    z-index: 2;
    transform: translate(-50%, -50%) rotate(-10deg) scaleY(0.32);
    pointer-events: none;
  }

  .shell-outer {
    width: 142%;
    height: 142%;
    border: 1px solid rgba(150, 135, 105, 0.055);
    box-shadow:
      0 0 38px rgba(0, 0, 0, 0.34),
      inset 0 0 46px rgba(0, 0, 0, 0.36),
      0 0 38px rgba(134, 87, 46, 0.055);
    opacity: 0.54;
  }

  .shell-inner {
    width: 92%;
    height: 92%;
    border: 1px solid rgba(166, 146, 109, 0.058);
    box-shadow:
      inset 0 0 42px rgba(0, 0, 0, 0.44),
      0 0 28px rgba(145, 100, 57, 0.06);
    opacity: 0.5;
  }

  .shadow-sheet {
    inset: 2%;
    z-index: 3;
    border-radius: 50%;
    background:
      radial-gradient(circle at 48% 47%, rgba(0, 0, 0, 0.02) 0 23%, rgba(0, 0, 0, 0.56) 45%, transparent 72%),
      radial-gradient(ellipse at 54% 61%, rgba(0, 0, 0, 0.54), transparent 56%),
      conic-gradient(
        from 72deg,
        rgba(0, 0, 0, 0.24) 0 12%,
        rgba(92, 58, 31, 0.11),
        transparent 30% 58%,
        rgba(0, 0, 0, 0.5),
        transparent 84% 100%
      );
    filter: blur(3px);
    opacity: 0.8;
  }

  .accretion-disc {
    top: 39%;
    right: 6%;
    left: 6%;
    height: 22%;
    border-radius: 50%;
    mix-blend-mode: screen;
    transform-origin: 50% 50%;
  }

  .accretion-disc::before,
  .accretion-disc::after {
    content: '';
    position: absolute;
    right: 2%;
    left: 2%;
    border-radius: inherit;
    pointer-events: none;
  }

  .accretion-disc::before {
    top: 42%;
    height: 10%;
    background: linear-gradient(90deg, transparent, rgba(244, 194, 118, 0.4), rgba(132, 70, 39, 0.18), transparent);
    filter: blur(2px);
  }

  .accretion-disc::after {
    top: 58%;
    height: 28%;
    background: linear-gradient(90deg, transparent, rgba(0, 0, 0, 0.58), transparent);
    filter: blur(2.4px);
    mix-blend-mode: multiply;
  }

  .disc-back {
    z-index: 4;
    background:
      radial-gradient(
        ellipse at 50% 47%,
        transparent 0 18%,
        rgba(154, 93, 45, 0.22) 30%,
        rgba(55, 29, 17, 0.24) 51%,
        transparent 72%
      ),
      conic-gradient(
        from 16deg,
        transparent,
        rgba(174, 108, 55, 0.28),
        transparent 26%,
        rgba(76, 94, 89, 0.11),
        transparent 62%
      );
    filter: blur(3px);
    opacity: 0.96;
    transform: rotate(-13deg) scaleY(0.34);
    animation: disc-breathe-back 7.5s ease-in-out infinite;
  }

  .disc-front {
    z-index: 8;
    background:
      radial-gradient(
        ellipse at 50% 48%,
        transparent 0 20%,
        rgba(234, 166, 88, 0.4) 32%,
        rgba(95, 43, 22, 0.32) 48%,
        transparent 72%
      ),
      conic-gradient(
        from 206deg,
        transparent,
        rgba(226, 156, 82, 0.42),
        rgba(93, 37, 20, 0.2),
        transparent 38%,
        rgba(148, 99, 56, 0.22),
        transparent 78%
      );
    filter: blur(1.1px) drop-shadow(0 12px 30px rgba(0, 0, 0, 0.66));
    opacity: 0.9;
    transform: rotate(-11deg) scaleY(0.28);
    mask-image: linear-gradient(180deg, transparent 0 36%, #000 47% 100%);
    animation: disc-breathe-front 5.8s ease-in-out infinite;
  }

  .photon-ring {
    top: 50%;
    left: 50%;
    z-index: 7;
    width: 38%;
    aspect-ratio: 1;
    border: 1px solid rgba(186, 157, 105, 0.12);
    border-radius: 50%;
    box-shadow:
      0 0 18px rgba(0, 0, 0, 0.42),
      0 0 34px rgba(132, 85, 44, 0.105),
      inset 0 0 24px rgba(164, 113, 64, 0.105),
      inset -22px -16px 28px rgba(0, 0, 0, 0.52);
    transform: translate(-50%, -50%);
    opacity: 0.48;
    animation: lens-pulse 12s ease-in-out infinite;
  }

  .event-horizon {
    top: 50%;
    left: 50%;
    z-index: 6;
    width: 30%;
    aspect-ratio: 1;
    border-radius: 50%;
    background:
      radial-gradient(circle at 36% 27%, rgba(204, 164, 105, 0.075) 0 2%, transparent 12%),
      radial-gradient(
        circle at 52% 50%,
        #000 0 40%,
        rgba(3, 3, 3, 0.99) 52%,
        rgba(17, 11, 8, 0.95) 66%,
        rgba(76, 40, 21, 0.24) 78%,
        transparent 87%
      ),
      conic-gradient(
        from 38deg,
        rgba(113, 72, 36, 0.22),
        transparent 17% 51%,
        rgba(61, 85, 80, 0.06),
        transparent 75% 100%
      );
    box-shadow:
      0 0 44px rgba(0, 0, 0, 0.9),
      0 0 68px rgba(27, 15, 9, 0.72),
      18px 24px 70px rgba(0, 0, 0, 0.86),
      inset -26px -19px 42px rgba(0, 0, 0, 0.99),
      inset 11px 8px 22px rgba(181, 127, 70, 0.06);
    transform: translate(-50%, -50%);
  }

  .event-horizon::before,
  .event-horizon::after {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: inherit;
    pointer-events: none;
  }

  .event-horizon::before {
    border: 1px solid rgba(141, 124, 97, 0.095);
    box-shadow:
      inset 8px 5px 14px rgba(126, 96, 62, 0.025),
      inset -16px -16px 28px rgba(0, 0, 0, 0.86);
    opacity: 0.72;
  }

  .event-horizon::after {
    background:
      radial-gradient(circle at 35% 28%, rgba(155, 120, 78, 0.045), transparent 18%),
      linear-gradient(135deg, rgba(102, 73, 45, 0.05), transparent 38%),
      conic-gradient(from 120deg, transparent 0 62%, rgba(91, 62, 38, 0.08), transparent 82% 100%);
    mix-blend-mode: screen;
    opacity: 0.66;
  }

  .near-side-shadow {
    top: 50%;
    left: 50%;
    z-index: 8;
    width: 46%;
    height: 17%;
    border-radius: 50%;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.66), rgba(0, 0, 0, 0.28), transparent);
    filter: blur(3.2px);
    mix-blend-mode: multiply;
    transform: translate(-50%, -34%) rotate(-11deg) scaleY(0.75);
    opacity: 0.78;
  }

  .event-shadow {
    top: 50%;
    left: 50%;
    z-index: 9;
    width: 21%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle at 42% 37%, rgba(12, 8, 4, 0.9), #000 62%), #000;
    box-shadow:
      0 0 38px 22px rgba(0, 0, 0, 0.88),
      inset 14px 10px 22px rgba(110, 83, 57, 0.018);
    transform: translate(-50%, -50%);
  }

  @keyframes gravity-turn {
    from {
      transform: rotate(0deg) scale(1);
    }

    50% {
      transform: rotate(180deg) scale(1.01);
    }

    to {
      transform: rotate(360deg) scale(1);
    }
  }

  @keyframes warp-back-breathe {
    0%,
    100% {
      opacity: 0.76;
      transform: translate(-50%, -61%) rotate(-12deg) skewX(-8deg) scaleY(0.48);
    }

    50% {
      opacity: 0.9;
      transform: translate(-50%, -58%) rotate(-10deg) skewX(-6deg) scaleY(0.53);
    }
  }

  @keyframes warp-front-breathe {
    0%,
    100% {
      opacity: 0.7;
      transform: translate(-50%, -50%) rotate(-10deg) skewX(8deg) scaleY(0.44);
    }

    50% {
      opacity: 0.84;
      transform: translate(-50%, -48%) rotate(-12deg) skewX(5deg) scaleY(0.5);
    }
  }

  @keyframes well-breathe {
    0%,
    100% {
      opacity: 0.84;
      transform: rotate(-11deg) scaleY(0.54) scale(0.99);
    }

    50% {
      opacity: 0.96;
      transform: rotate(-9deg) scaleY(0.58) scale(1.015);
    }
  }

  @keyframes disc-breathe-back {
    0%,
    100% {
      opacity: 0.66;
      filter: blur(3.2px);
    }

    52% {
      opacity: 0.8;
      filter: blur(2.8px);
    }
  }

  @keyframes disc-breathe-front {
    0%,
    100% {
      opacity: 0.76;
      filter: blur(1.3px);
    }

    48% {
      opacity: 0.88;
      filter: blur(1px);
    }
  }

  @keyframes lens-pulse {
    0%,
    100% {
      opacity: 0.34;
      transform: translate(-50%, -50%) scale(0.98);
    }

    52% {
      opacity: 0.5;
      transform: translate(-50%, -50%) scale(1.04);
    }
  }

  @media (max-width: 960px) {
    .ember-stage {
      --hole-x: 66%;
      --hole-size: min(60vw, 600px);

      inset: 0 -22vw 0 18%;
      opacity: 0.72;
    }
  }

  @media (max-width: 720px) {
    .ember-stage {
      --hole-x: 52%;
      --hole-y: 43%;
      --hole-size: min(96vw, 410px);

      inset: 0 -58vw 0 3%;
      opacity: 0.64;
      mask-image: linear-gradient(180deg, #000 0%, #000 68%, transparent 100%);
    }

    .ember-stage::after {
      opacity: 0.76;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .gravity-halo,
    .depth-well,
    .warp-back,
    .warp-front,
    .disc-back,
    .disc-front,
    .photon-ring {
      animation: none;
    }
  }
</style>

<script>
  import './emberFieldCanvas';
</script>
