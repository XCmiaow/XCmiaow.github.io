<div class="ember-stage" aria-hidden="true">
  <div class="cosmic-grain"></div>
  <div class="black-hole-scene">
    <div class="gravity-veil"></div>
    <div class="lensing-arc arc-back"></div>
    <div class="accretion-disc disc-back"></div>
    <div class="photon-ring"></div>
    <div class="event-horizon"></div>
    <div class="accretion-disc disc-front"></div>
    <div class="lensing-arc arc-front"></div>
  </div>
  <canvas data-ember-canvas></canvas>
  <div class="field-caption"><span>FIELD 01</span><span>gravity / memory</span></div>
</div>

<style>
  .ember-stage {
    --hole-x: 72%;
    --hole-y: 50%;
    --hole-size: min(47vw, 680px);

    position: absolute;
    inset: 0 max(-7vw, -104px) 0 29%;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
    mask-image: linear-gradient(90deg, transparent 0%, #000 13%, #000 88%, transparent 100%);
  }

  .ember-stage::before,
  .ember-stage::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .ember-stage::before {
    z-index: 1;
    background:
      radial-gradient(circle at 72% 48%, transparent 0 12%, rgba(203, 128, 66, 0.055) 31%, transparent 58%),
      linear-gradient(90deg, rgba(7, 6, 4, 0.96) 0%, rgba(7, 6, 4, 0.56) 32%, transparent 64%);
  }

  .ember-stage::after {
    z-index: 9;
    background: linear-gradient(90deg, rgba(7, 6, 4, 0.92) 0%, rgba(7, 6, 4, 0.28) 30%, transparent 57%);
  }

  .cosmic-grain {
    position: absolute;
    inset: 7% 0 9% 15%;
    z-index: 0;
    background-image:
      radial-gradient(circle at 14% 28%, rgba(244, 220, 177, 0.34) 0 0.6px, transparent 1.1px),
      radial-gradient(circle at 68% 18%, rgba(230, 183, 124, 0.3) 0 0.7px, transparent 1.2px),
      radial-gradient(circle at 87% 72%, rgba(244, 220, 177, 0.24) 0 0.6px, transparent 1.2px),
      radial-gradient(circle at 43% 81%, rgba(210, 137, 77, 0.28) 0 0.8px, transparent 1.4px);
    background-size:
      163px 149px,
      211px 197px,
      241px 223px,
      179px 191px;
    opacity: 0.38;
    mask-image: radial-gradient(ellipse at 68% 50%, #000 0 35%, transparent 77%);
  }

  canvas {
    position: absolute;
    inset: 0;
    z-index: 8;
    width: 100%;
    height: 100%;
  }

  .black-hole-scene,
  .gravity-veil,
  .lensing-arc,
  .accretion-disc,
  .photon-ring,
  .event-horizon {
    position: absolute;
  }

  .black-hole-scene {
    top: var(--hole-y);
    left: var(--hole-x);
    z-index: 3;
    width: var(--hole-size);
    aspect-ratio: 1;
    transform: translate(-50%, -50%);
    transform-style: preserve-3d;
  }

  .black-hole-scene::before {
    content: '';
    position: absolute;
    inset: -9%;
    z-index: -2;
    border: 1px solid rgba(225, 176, 112, 0.08);
    border-radius: 50%;
    opacity: 0.65;
    transform: rotate(-11deg) scaleY(0.92);
  }

  .gravity-veil {
    inset: -30%;
    z-index: -3;
    border-radius: 50%;
    background:
      radial-gradient(circle at 50% 50%, rgba(0, 0, 0, 0.9) 0 14%, rgba(0, 0, 0, 0.45) 23%, transparent 45%),
      radial-gradient(ellipse at 52% 49%, rgba(189, 109, 50, 0.15) 0 12%, rgba(83, 48, 26, 0.11) 32%, transparent 66%);
    filter: blur(24px);
    opacity: 0.9;
    transform: rotate(-12deg) scale(1.18, 0.82);
    animation: veil-breathe 16s ease-in-out infinite;
  }

  .lensing-arc {
    top: 50%;
    left: 50%;
    width: 68%;
    height: 68%;
    border-radius: 50%;
    transform-origin: center;
  }

  .arc-back {
    z-index: 1;
    border-top: 1px solid rgba(242, 201, 140, 0.5);
    border-left: 1px solid rgba(197, 119, 57, 0.16);
    filter: drop-shadow(0 -4px 12px rgba(200, 117, 55, 0.14));
    clip-path: inset(0 0 48% 0);
    transform: translate(-50%, -50%) rotate(-13deg) scale(1.24, 0.98);
    opacity: 0.62;
  }

  .arc-front {
    z-index: 7;
    border-right: 1px solid rgba(229, 158, 91, 0.3);
    border-bottom: 1px solid rgba(239, 186, 117, 0.52);
    filter: drop-shadow(0 5px 13px rgba(205, 119, 57, 0.12));
    clip-path: inset(48% 0 0 0);
    transform: translate(-50%, -50%) rotate(-13deg) scale(1.18, 0.95);
    opacity: 0.5;
  }

  .accretion-disc {
    top: 50%;
    left: 50%;
    width: 146%;
    height: 42%;
    border-radius: 50%;
    background:
      radial-gradient(
        ellipse at center,
        transparent 0 29%,
        rgba(255, 222, 159, 0.58) 34%,
        rgba(211, 126, 59, 0.31) 43%,
        transparent 61%
      ),
      conic-gradient(
        from 188deg at 50% 50%,
        transparent 0 10%,
        rgba(128, 67, 31, 0.1) 18%,
        rgba(234, 161, 88, 0.48) 31%,
        rgba(255, 221, 151, 0.78) 39%,
        rgba(163, 84, 38, 0.18) 51%,
        transparent 61% 78%,
        rgba(205, 118, 51, 0.24) 91%,
        transparent
      );
    filter: blur(1px);
    mix-blend-mode: screen;
    mask-image: radial-gradient(ellipse at center, transparent 0 28%, #000 34% 48%, transparent 62%);
    transform: translate(-50%, -50%) rotate(-12deg) scaleY(0.5);
  }

  .disc-back {
    z-index: 2;
    clip-path: inset(0 0 49% 0);
    opacity: 0.66;
    animation: disc-drift 28s ease-in-out infinite;
  }

  .disc-front {
    z-index: 6;
    clip-path: inset(49% 0 0 0);
    opacity: 0.88;
    transform: translate(-50%, -50%) rotate(-12deg) scaleY(0.54);
    animation: disc-drift-front 21s ease-in-out infinite;
  }

  .photon-ring {
    top: 50%;
    left: 50%;
    z-index: 4;
    width: 37%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: conic-gradient(
      from 214deg,
      rgba(122, 66, 32, 0.14),
      rgba(245, 190, 114, 0.9) 20%,
      rgba(255, 225, 163, 0.98) 28%,
      rgba(188, 103, 45, 0.28) 47%,
      rgba(88, 47, 27, 0.12) 67%,
      rgba(228, 147, 74, 0.62) 86%,
      rgba(122, 66, 32, 0.14)
    );
    filter: blur(0.35px) drop-shadow(-8px -2px 12px rgba(219, 139, 68, 0.24));
    mask-image: radial-gradient(circle, transparent 0 40%, #000 44% 51%, transparent 56%);
    transform: translate(-50%, -50%);
    animation: photon-turn 44s linear infinite;
  }

  .event-horizon {
    top: 50%;
    left: 50%;
    z-index: 5;
    width: 29%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle at 41% 34%, #060504 0 23%, #010101 58%, #000 100%);
    box-shadow:
      inset -11px -14px 32px rgba(0, 0, 0, 0.94),
      inset 7px 5px 18px rgba(49, 31, 19, 0.18),
      0 0 0 1px rgba(235, 177, 104, 0.08),
      0 0 42px rgba(0, 0, 0, 0.9);
    transform: translate(-50%, -50%);
  }

  .event-horizon::after {
    content: '';
    position: absolute;
    inset: 13%;
    border-radius: 50%;
    background: #000;
    box-shadow: inset 6px 7px 18px rgba(31, 20, 13, 0.12);
  }

  .field-caption {
    position: absolute;
    right: clamp(3rem, 8vw, 8rem);
    bottom: clamp(2rem, 7vh, 5rem);
    z-index: 10;
    display: grid;
    gap: 0.3rem;
    padding-left: 1rem;
    border-left: 1px solid rgba(229, 173, 103, 0.38);
    color: rgba(230, 209, 175, 0.42);
    font-family: Consolas, 'SFMono-Regular', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .field-caption span:last-child {
    color: rgba(230, 209, 175, 0.24);
    letter-spacing: 0.06em;
  }

  @keyframes photon-turn {
    to {
      transform: translate(-50%, -50%) rotate(360deg);
    }
  }

  @keyframes veil-breathe {
    0%,
    100% {
      opacity: 0.76;
      transform: rotate(-12deg) scale(1.14, 0.8);
    }
    50% {
      opacity: 0.94;
      transform: rotate(-10deg) scale(1.2, 0.85);
    }
  }

  @keyframes disc-drift {
    0%,
    100% {
      opacity: 0.58;
      transform: translate(-50%, -50%) rotate(-12deg) scaleY(0.5);
    }
    50% {
      opacity: 0.7;
      transform: translate(-50%, -50%) rotate(-10deg) scaleY(0.52);
    }
  }

  @keyframes disc-drift-front {
    0%,
    100% {
      opacity: 0.82;
      transform: translate(-50%, -50%) rotate(-12deg) scaleY(0.54);
    }
    50% {
      opacity: 0.92;
      transform: translate(-50%, -50%) rotate(-13.5deg) scaleY(0.56);
    }
  }

  @media (max-width: 960px) {
    .ember-stage {
      --hole-x: 73%;
      --hole-size: min(62vw, 600px);

      inset: 0 -22vw 0 14%;
      opacity: 0.82;
    }

    .field-caption {
      right: 18vw;
    }
  }

  @media (max-width: 720px) {
    .ember-stage {
      --hole-x: 72%;
      --hole-y: 29%;
      --hole-size: min(108vw, 430px);

      inset: 0 -52vw 0 4%;
      opacity: 0.66;
      mask-image: linear-gradient(180deg, #000 0%, #000 57%, transparent 82%);
    }

    .ember-stage::after {
      background: linear-gradient(90deg, rgba(7, 6, 4, 0.9) 0%, rgba(7, 6, 4, 0.4) 46%, transparent 78%);
    }

    .field-caption {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .gravity-veil,
    .disc-back,
    .disc-front,
    .photon-ring {
      animation: none;
    }
  }
</style>

<script>
  import './emberFieldCanvas';
</script>
