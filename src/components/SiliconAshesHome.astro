---
import common from '../data/common.json';
import { getCollection } from 'astro:content';

export interface Props {
  lang: 'zh' | 'en';
}


const { lang } = Astro.props;

const latestPosts = (await getCollection('blog', ({ data }) => {
  return data.lang === lang && !data.draft;
})).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf()).slice(0, 3);

const formatDate = (date: Date) => {
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, '0');
  const d = String(date.getDate()).padStart(2, '0');
  return `${y}-${m}-${d}`;
};
const isZh = lang === 'zh';
const localizedRoot = isZh ? '' : '/en';
const homeHref = isZh ? '/' : '/en/';
const email = common.person.email;
const github = common.person.github;
const githubUrl = `https://${github}`;
const courseBase = '/silicon-ashes/courses/ai-research-efficiency/';
const courseIndex = `${courseBase}index.html`;

const t = isZh
  ? {
      htmlLabel: '个人品牌主站',
      brand: '硅基余烬',
      switchHref: '/en/silicon-ashes/',
      switchLabel: 'EN',
      nav: [
        ['写作', isZh ? '/silicon-ashes/writing/' : '/en/silicon-ashes/writing/'],
        ['课程', isZh ? '/silicon-ashes/courses/' : '/en/silicon-ashes/courses/'],
        ['资源', isZh ? '/silicon-ashes/resources/' : '/en/silicon-ashes/resources/'],
        ['关于', isZh ? '/silicon-ashes/about/' : '/en/silicon-ashes/about/'],
      ],
      eyebrow: 'personal brand',
      title: '硅基余烬',
      lead: '在技术狂奔之后，留下人的判断力。',
      primary: '读最新写作',
      secondary: '课程与资源',
      heroNotes: ['技术批判', 'AI 工作流', '科研表达'],
      manifestoKicker: 'brand thesis',
      manifestoTitle: '硅基之后，还有余烬。',
      manifestoBody: '硅基是速度，余烬是判断。不反技术，反对停止思考。',
      creed: [
        ['不迷信技术', '工具越强，越需要判断力。'],
        ['不美化效率', '效率必须经得起人的追问。'],
        ['不把人写成背景', '技术叙事的主角仍然是人。'],
      ],
      coursesKicker: 'course section',
      coursesTitle: '把 AI 方法做成可进入的课程。',
      coursesIntro: '面向非计算机背景，少讲玄学，多做工作流。',
      courseName: 'AI 编程与科研效率基础课',
      courseBody: '8 小时起步。目标不是学会所有工具，而是搭出自己的可复用流程。',
      courseFacts: [
        ['对象', '教师与研究者'],
        ['方式', '工具链实操'],
        ['交付', '模板、清单、工作流'],
      ],
      courseLinks: [
        ['全部课程', `${localizedRoot}/silicon-ashes/courses/`],
        ['课程首页', courseIndex],
        ['课程安排', `${courseBase}schedule.html`],
        ['工具实操', `${courseBase}toolchain.html`],
        ['打印讲义', `${courseBase}handout.html`],
        ['PDF 讲义', `${courseBase}course-handout.pdf`],
      ],
      profileKicker: 'profile',
      profileTitle: '入口',
      profileIntro: '需要简历、材料或证明，从这里走。',
      profileLinks: [
        ['个人简历首页', homeHref],
        ['通用一页版', `${localizedRoot}/resume-onepage`],
        ['保研评优版', `${localizedRoot}/resume-academic`],
        ['求职实习版', `${localizedRoot}/resume-career`],
        ['资源与工具', `${localizedRoot}/silicon-ashes/resources/`],
        ['材料中心', `${localizedRoot}/materials`],
        ['证明材料', `${localizedRoot}/evidence`],
      ],
      footerNote: '硅基余烬。',
    }
  : {
      htmlLabel: 'personal brand site',
      brand: 'Silicon Ashes',
      switchHref: '/silicon-ashes/',
      switchLabel: '中文',
      nav: [
        ['Writing', isZh ? '/silicon-ashes/writing/' : '/en/silicon-ashes/writing/'],
        ['Course', isZh ? '/silicon-ashes/courses/' : '/en/silicon-ashes/courses/'],
        ['Resources', isZh ? '/silicon-ashes/resources/' : '/en/silicon-ashes/resources/'],
        ['About', isZh ? '/silicon-ashes/about/' : '/en/silicon-ashes/about/'],
      ],
      eyebrow: 'personal brand',
      title: 'Silicon Ashes',
      lead: 'Keep human judgment after technology starts running.',
      primary: 'Read latest writing',
      secondary: 'Courses and resources',
      heroNotes: ['Technology criticism', 'AI workflows', 'Research expression'],
      manifestoKicker: 'brand thesis',
      manifestoTitle: 'After silicon, there are still ashes.',
      manifestoBody: 'Silicon is speed. Ashes are judgment. Not anti-technology, anti-surrender.',
      creed: [
        ['Do not worship technology', 'Stronger tools demand stronger judgment.'],
        ['Do not romanticize efficiency', 'Efficiency still needs a human question.'],
        ['Do not turn people into background', 'People remain the subject of technical stories.'],
      ],
      coursesKicker: 'course section',
      coursesTitle: 'AI methods, shaped as teachable workflows.',
      coursesIntro: 'For non-CS learners: fewer myths, more working systems.',
      courseName: 'AI Programming and Research Efficiency Basics',
      courseBody: 'Starts at 8 hours. The goal is a reusable personal workflow, not a pile of tools.',
      courseFacts: [
        ['Audience', 'Teachers and researchers'],
        ['Mode', 'Hands-on toolchain'],
        ['Output', 'Templates, checklists, workflow'],
      ],
      courseLinks: [
        ['All courses', `${localizedRoot}/silicon-ashes/courses/`],
        ['Course home', courseIndex],
        ['Schedule', `${courseBase}schedule.html`],
        ['Toolchain', `${courseBase}toolchain.html`],
        ['Printable handout', `${courseBase}handout.html`],
        ['PDF handout', `${courseBase}course-handout.pdf`],
      ],
      profileKicker: 'profile',
      profileTitle: 'Entry points',
      profileIntro: 'Resume, materials, and proof pages.',
      profileLinks: [
        ['Resume home', homeHref],
        ['One-page resume', `${localizedRoot}/resume-onepage`],
        ['Academic resume', `${localizedRoot}/resume-academic`],
        ['Career resume', `${localizedRoot}/resume-career`],
        ['Resources', `${localizedRoot}/silicon-ashes/resources/`],
        ['Materials hub', `${localizedRoot}/materials`],
        ['Evidence gallery', `${localizedRoot}/evidence`],
      ],
      footerNote: 'Silicon Ashes.',
    };
---

<div class="sa-shell" data-lang={lang}>
  <a class="skip-link" href="#main">{isZh ? '跳到正文' : 'Skip to content'}</a>

  <header class="sa-nav" aria-label={t.htmlLabel}>
    <a class="sa-brand" href={isZh ? '/silicon-ashes/' : '/en/silicon-ashes/'} aria-label={t.brand}>
      <span class="brand-mark">SA</span>
      <span class="brand-name">{t.brand}</span>
    </a>
    <nav aria-label={isZh ? '主站导航' : 'Main navigation'}>
      {t.nav.map(([label, href]) => <a href={href}>{label}</a>)}
      <a href={t.switchHref}>{t.switchLabel}</a>
    </nav>
  </header>

  <main id="main">
    <section class="hero" aria-labelledby="sa-title">
      <div class="flame-field" aria-hidden="true">
        <canvas class="flame-canvas" data-flame-canvas></canvas>
        <div class="flame-body"></div>
      </div>
      <div class="hero-copy">
        <p class="eyebrow">{t.eyebrow}</p>
        <h1 id="sa-title">{t.title}</h1>
        <p class="lead">{t.lead}</p>
        <div class="actions" aria-label={isZh ? '主要入口' : 'Primary links'}>
          <a class="sa-button primary" href={isZh ? '/silicon-ashes/writing/' : '/en/silicon-ashes/writing/'}>{t.primary}</a>
          <a class="sa-button quiet" href={isZh ? '/silicon-ashes/resources/' : '/en/silicon-ashes/resources/'}>{t.secondary}</a>
        </div>
      </div>
      <aside class="hero-aside" aria-label={isZh ? '品牌关键词' : 'Brand keywords'}>
        <p>{isZh ? '少一点未来感，多一点人的处境。' : 'Less futurism. More human consequence.'}</p>
        <div>
          {t.heroNotes.map((item) => <span>{item}</span>)}
        </div>
      </aside>
    </section>

    <section id="manifesto" class="manifesto">
      <div class="section-copy">
        <p class="kicker">{t.manifestoKicker}</p>
        <h2>{t.manifestoTitle}</h2>
      </div>
      <div class="manifesto-body">
        <p>{t.manifestoBody}</p>
        <div class="creed-list">
          {
            t.creed.map(([title, body]) => (
              <article>
                <h3>{title}</h3>
                <p>{body}</p>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    {latestPosts.length > 0 && (
      <section class="writing-preview" aria-labelledby="writing-title">
        <div class="section-copy wide">
          <p class="kicker">{isZh ? '最新写作' : 'Latest writing'}</p>
          <h2 id="writing-title">{isZh ? '最新文章' : 'Recent articles'}</h2>
        </div>
        <div class="writing-list">
          {latestPosts.map((post) => (
            <a class="writing-card" href={isZh ? `/silicon-ashes/writing/${post.id}/` : `/en/silicon-ashes/writing/${post.id}/`}>
              <div class="writing-meta">
                <time datetime={post.data.date.toISOString()}>{formatDate(post.data.date)}</time>
                {post.data.readingTime && <span>{post.data.readingTime} min</span>}
              </div>
              <h3>{post.data.title}</h3>
              {post.data.description && <p>{post.data.description}</p>}
            </a>
          ))}
        </div>
        <div class="writing-more">
          <a href={isZh ? '/silicon-ashes/writing/' : '/en/silicon-ashes/writing/'}>{isZh ? '全部文章 →' : 'All articles →'}</a>
        </div>
      </section>
    )}

    <section id="courses" class="courses" aria-labelledby="courses-title">
      <div class="section-copy wide">
        <p class="kicker">{t.coursesKicker}</p>
        <h2 id="courses-title">{t.coursesTitle}</h2>
        <p>{t.coursesIntro}</p>
      </div>
      <article class="course-panel">
        <div class="course-main">
          <p class="course-label">{isZh ? '已接入课程' : 'Connected course'}</p>
          <h3>{t.courseName}</h3>
          <p>{t.courseBody}</p>
          <div class="course-actions">
            {t.courseLinks.map(([label, href]) => <a href={href}>{label}</a>)}
          </div>
        </div>
        <dl class="course-facts">
          {
            t.courseFacts.map(([label, value]) => (
              <div>
                <dt>{label}</dt>
                <dd>{value}</dd>
              </div>
            ))
          }
        </dl>
      </article>
    </section>

    <section id="profile" class="profile" aria-labelledby="profile-title">
      <div class="section-copy wide">
        <p class="kicker">{t.profileKicker}</p>
        <h2 id="profile-title">{t.profileTitle}</h2>
        <p>{t.profileIntro}</p>
      </div>
      <div class="profile-links">
        {t.profileLinks.map(([label, href]) => <a href={href}>{label}</a>)}
      </div>
    </section>
  </main>

  <footer class="sa-footer">
    <p>{t.footerNote}</p>
    <div>
      <a href={`mailto:${email}`}>{email}</a>
      <a href={githubUrl} target="_blank" rel="noreferrer">{github}</a>
    </div>
  </footer>
</div>

﻿<script>
  const flameCanvas = document.querySelector("[data-flame-canvas]");

  if (flameCanvas instanceof HTMLCanvasElement) {
    const host = flameCanvas.parentElement;
    const context = flameCanvas.getContext("2d", { alpha: true });
    const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");

    if (host && context) {
      const particles = [];
      let width = 0;
      let height = 0;
      let frameId = 0;
      let lastTime = performance.now();

      const rand = (min, max) => min + Math.random() * (max - min);

      const resize = () => {
        const rect = host.getBoundingClientRect();
        const ratio = Math.min(window.devicePixelRatio || 1, 2);
        width = Math.max(1, rect.width);
        height = Math.max(1, rect.height);
        flameCanvas.width = Math.floor(width * ratio);
        flameCanvas.height = Math.floor(height * ratio);
        flameCanvas.style.width = width + "px";
        flameCanvas.style.height = height + "px";
        context.setTransform(ratio, 0, 0, ratio, 0, 0);
      };

      /* ── particle types: 0=flame, 1=ember, 2=spark, 3=smoke ── */

      const emitFlame = (time) => {
        const cx = width * 0.54 + Math.sin(time * 0.0012) * width * 0.04;
        particles.push({
          type: 0,
          x: cx + rand(-width * 0.18, width * 0.18),
          y: height * rand(0.74, 0.92),
          vx: rand(-0.6, 0.6),
          vy: rand(-3.2, -1.2),
          size: rand(3, 11),
          life: 0,
          maxLife: rand(900, 2000),
          heat: rand(0, 1),
          drift: rand(0.002, 0.005),
          seed: rand(0, 1000),
        });
      };

      const emitEmber = () => {
        particles.push({
          type: 1,
          x: width * 0.54 + rand(-width * 0.12, width * 0.12),
          y: height * rand(0.6, 0.88),
          vx: rand(-0.3, 0.3),
          vy: rand(-1.6, -0.5),
          size: rand(1.2, 3.2),
          life: 0,
          maxLife: rand(1800, 3800),
          heat: rand(0.6, 1),
          drift: rand(0.001, 0.003),
          seed: rand(0, 1000),
        });
      };

      const emitSpark = () => {
        particles.push({
          type: 2,
          x: width * 0.54 + rand(-width * 0.08, width * 0.08),
          y: height * rand(0.5, 0.82),
          vx: rand(-1.8, 1.8),
          vy: rand(-6, -3),
          size: rand(0.6, 2),
          life: 0,
          maxLife: rand(600, 1400),
          heat: 1,
          drift: rand(0.003, 0.008),
          seed: rand(0, 1000),
          gravity: rand(0.008, 0.018),
        });
      };

      const emitSmoke = () => {
        particles.push({
          type: 3,
          x: width * 0.54 + rand(-width * 0.14, width * 0.14),
          y: height * rand(0.25, 0.55),
          vx: rand(-0.15, 0.15),
          vy: rand(-0.8, -0.25),
          size: rand(18, 48),
          life: 0,
          maxLife: rand(2600, 5000),
          heat: 0,
          drift: rand(0.0008, 0.002),
          seed: rand(0, 1000),
        });
      };

      /* ── draw flame body (inner glow, 4 layers) ── */
      const drawBody = (time) => {
        const cx = width * 0.54;
        const by = height * 0.92;
        const wave = Math.sin(time * 0.0018) * width * 0.03;
        context.save();
        context.globalCompositeOperation = "lighter";

        const layerData = [
          { spread: 0.38, topBase: 0.12, alpha: 0.14, colors: ["255,250,220", "255,200,80", "220,80,20"] },
          { spread: 0.28, topBase: 0.20, alpha: 0.20, colors: ["255,235,170", "235,100,30", "200,60,15"] },
          { spread: 0.18, topBase: 0.30, alpha: 0.28, colors: ["255,210,100", "200,60,15", "150,35,10"] },
          { spread: 0.09, topBase: 0.40, alpha: 0.36, colors: ["255,245,200", "255,160,50", "180,40,10"] },
        ];

        for (let layer = 0; layer < 4; layer += 1) {
          const d = layerData[layer];
          const spread = d.spread * width;
          const top = height * (d.topBase + Math.sin(time * 0.0015 + layer * 1.3) * 0.02);
          const g = context.createLinearGradient(cx, top, cx, by);
          g.addColorStop(0, "rgba(" + d.colors[0] + "," + d.alpha + ")");
          g.addColorStop(0.35, "rgba(" + d.colors[1] + "," + (d.alpha * 0.85) + ")");
          g.addColorStop(0.7, "rgba(" + d.colors[2] + "," + (d.alpha * 0.4) + ")");
          g.addColorStop(1, "rgba(100,20,8,0)");

          const wobble = wave * (layer + 1) * 0.22;
          context.beginPath();
          context.moveTo(cx + wobble, top);
          context.bezierCurveTo(
            cx - spread * 0.38, height * (0.38 + layer * 0.04),
            cx - spread, height * 0.66, cx, by
          );
          context.bezierCurveTo(
            cx + spread, height * 0.66,
            cx + spread * 0.34, height * (0.37 + layer * 0.04),
            cx + wobble * 0.8, top
          );
          context.closePath();
          context.fillStyle = g;
          context.filter = "blur(" + (6 + layer * 5) + "px)";
          context.fill();
        }
        context.restore();
      };

      /* ── draw each particle type ── */
      const drawFlame = (p, time) => {
        const progress = p.life / p.maxLife;
        const alpha = Math.max(0, Math.pow(1 - progress, 1.5));
        const pulse = 1 + Math.sin(time * p.drift + p.seed) * 0.22;
        const r = p.size * (1 + progress * 2.8) * pulse;
        const g = context.createRadialGradient(p.x, p.y, 0, p.x, p.y, r * 2.8);
        g.addColorStop(0, "rgba(255,250,200," + (alpha * 0.8) + ")");
        g.addColorStop(0.18, "rgba(255,220,120," + (alpha * 0.55) + ")");
        g.addColorStop(0.4, "rgba(255," + Math.floor(130 + p.heat * 80) + ",40," + (alpha * 0.3) + ")");
        g.addColorStop(1, "rgba(120,30,8,0)");
        context.beginPath();
        context.fillStyle = g;
        context.arc(p.x, p.y, r * 2.8, 0, Math.PI * 2);
        context.fill();
      };

      const drawEmber = (p, time) => {
        const progress = p.life / p.maxLife;
        const alpha = Math.max(0, Math.pow(1 - progress, 0.9));
        const flicker = 0.7 + Math.sin(time * 0.006 + p.seed) * 0.3;
        const r = p.size * (1 + progress * 0.8);
        const g = context.createRadialGradient(p.x, p.y, 0, p.x, p.y, r * 4);
        g.addColorStop(0, "rgba(255,240,160," + (alpha * flicker) + ")");
        g.addColorStop(0.3, "rgba(255," + Math.floor(160 + p.heat * 60) + ",30," + (alpha * 0.4 * flicker) + ")");
        g.addColorStop(1, "rgba(160,40,10,0)");
        context.beginPath();
        context.fillStyle = g;
        context.arc(p.x, p.y, r * 4, 0, Math.PI * 2);
        context.fill();
      };

      const drawSpark = (p) => {
        const progress = p.life / p.maxLife;
        const alpha = Math.max(0, 1 - progress);
        context.save();
        context.globalAlpha = alpha;
        context.fillStyle = "#fff8e0";
        context.shadowColor = "rgba(255,200,60,0.8)";
        context.shadowBlur = 8;
        context.beginPath();
        context.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        context.fill();
        context.restore();
      };

      const drawSmoke = (p) => {
        const progress = p.life / p.maxLife;
        const alpha = Math.max(0, Math.pow(1 - progress, 0.6)) * 0.08;
        const r = p.size * (1 + progress * 1.8);
        const g = context.createRadialGradient(p.x, p.y, 0, p.x, p.y, r);
        g.addColorStop(0, "rgba(180,170,155," + alpha + ")");
        g.addColorStop(1, "rgba(120,110,100,0)");
        context.beginPath();
        context.fillStyle = g;
        context.arc(p.x, p.y, r, 0, Math.PI * 2);
        context.fill();
      };

      const renderStatic = () => {
        context.clearRect(0, 0, width, height);
        drawBody(performance.now());
      };

      const tick = (time) => {
        const delta = Math.min(40, time - lastTime);
        lastTime = time;
        context.clearRect(0, 0, width, height);
        drawBody(time);
        context.globalCompositeOperation = "lighter";

        /* emission rates */
        const narrow = width < 420;
        for (let i = 0; i < (narrow ? 4 : 8); i++) emitFlame(time);
        for (let i = 0; i < (narrow ? 2 : 4); i++) emitEmber();
        if (Math.random() < (narrow ? 0.15 : 0.3)) emitSpark();
        if (Math.random() < (narrow ? 0.06 : 0.12)) emitSmoke();

        /* update + draw */
        for (let i = particles.length - 1; i >= 0; i--) {
          const p = particles[i];
          p.life += delta;
          const turbX = Math.sin(time * p.drift + p.seed) * 0.6;
          const turbY = Math.cos(time * p.drift * 0.7 + p.seed * 0.5) * 0.15;
          const wind = Math.sin(time * 0.0004) * 0.25;
          p.x += p.vx + turbX + wind;
          p.y += p.vy + turbY;
          p.vx *= 0.99;
          p.vy *= p.type === 2 ? 0.985 : 0.993;
          if (p.type === 2 && p.gravity) p.vy += p.gravity;

          if (p.life >= p.maxLife || p.y < -80) {
            particles.splice(i, 1);
          } else if (p.type === 0) {
            drawFlame(p, time);
          } else if (p.type === 1) {
            drawEmber(p, time);
          } else if (p.type === 2) {
            drawSpark(p);
          } else {
            drawSmoke(p);
          }
        }
        while (particles.length > 600) particles.shift();
        frameId = window.requestAnimationFrame(tick);
      };

      const start = () => {
        window.cancelAnimationFrame(frameId);
        particles.length = 0;
        resize();
        if (reducedMotion.matches) {
          renderStatic();
          return;
        }
        lastTime = performance.now();
        frameId = window.requestAnimationFrame(tick);
      };

      const observer = new ResizeObserver(start);
      observer.observe(host);
      reducedMotion.addEventListener("change", start);
      window.addEventListener("pagehide", () => window.cancelAnimationFrame(frameId), { once: true });
      start();
    }
  }
</script>

<style>
  :global(html) {
    scroll-behavior: smooth;
    background: #080705;
  }

  :global(body) {
    margin: 0;
    background: #080705;
    color: #e9e0d0;
    font-family: 'Satoshi', 'Avenir Next', 'Segoe UI', 'Microsoft YaHei', sans-serif;
  }

  :global(*),
  :global(*::before),
  :global(*::after) {
    box-sizing: border-box;
  }

  :global(a) {
    color: inherit;
  }

  .sa-shell {
    min-height: 100dvh;
    overflow-x: hidden;
    background:
      linear-gradient(90deg, rgba(226, 187, 116, 0.035) 1px, transparent 1px),
      linear-gradient(180deg, rgba(226, 187, 116, 0.028) 1px, transparent 1px),
      linear-gradient(112deg, transparent 0 56%, rgba(168, 103, 38, 0.09) 56.2%, transparent 56.5%),
      linear-gradient(180deg, #080705 0%, #0e0d0b 42%, #070706 100%);
    background-size:
      64px 64px,
      64px 64px,
      auto,
      auto;
  }

  .skip-link {
    position: fixed;
    top: 12px;
    left: 12px;
    z-index: 20;
    transform: translateY(-160%);
    border: 1px solid #d6a04f;
    border-radius: 4px;
    padding: 10px 12px;
    background: #e3b66c;
    color: #100d09;
    font-weight: 800;
    text-decoration: none;
    transition: transform 180ms ease;
  }

  .skip-link:focus {
    transform: translateY(0);
  }

  .sa-nav,
  main,
  .sa-footer {
    width: min(100% - 44px, 1240px);
    margin: 0 auto;
  }

  .sa-nav {
    position: sticky;
    top: 0;
    z-index: 12;
    display: flex;
    min-height: 76px;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    border-bottom: 1px solid rgba(218, 166, 86, 0.2);
    background: rgba(8, 7, 5, 0.86);
    backdrop-filter: blur(18px);
  }

  .sa-brand {
    display: inline-flex;
    min-height: 40px;
    align-items: center;
    gap: 12px;
    text-decoration: none;
  }

  .brand-mark {
    display: grid;
    width: 40px;
    height: 40px;
    place-items: center;
    border: 1px solid rgba(218, 166, 86, 0.7);
    border-radius: 5px;
    background: linear-gradient(135deg, rgba(238, 181, 92, 0.22), transparent 50%), #11100d;
    color: #ffe4aa;
    font-family: Consolas, 'SFMono-Regular', monospace;
    font-size: 13px;
    font-weight: 900;
  }

  .brand-name {
    color: #f3e3c5;
    font-family: 'Source Han Serif SC', 'Noto Serif SC', 'Songti SC', serif;
    font-size: 20px;
    font-weight: 800;
  }

  .sa-nav nav {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .sa-nav nav a {
    display: inline-flex;
    min-height: 38px;
    align-items: center;
    border-radius: 4px;
    padding: 0 10px;
    color: rgba(233, 224, 208, 0.72);
    font-size: 14px;
    font-weight: 700;
    text-decoration: none;
    transition:
      background 160ms ease,
      color 160ms ease;
  }

  .sa-nav nav a:hover,
  .sa-nav nav a:focus-visible {
    background: rgba(218, 166, 86, 0.1);
    color: #ffe2aa;
    outline: none;
  }

  .hero {
    position: relative;
    isolation: isolate;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(360px, 0.55fr);
    gap: 58px;
    align-items: center;
    min-height: min(820px, calc(100dvh - 76px));
    padding: 58px 0 78px;
  }

  .hero-copy {
    position: relative;
    z-index: 2;
  }

  .flame-field {
    position: absolute;
    right: max(-9vw, -130px);
    bottom: 24px;
    z-index: 0;
    width: min(56vw, 710px);
    height: min(74vh, 720px);
    min-height: 440px;
    pointer-events: none;
    opacity: 0.9;
    mix-blend-mode: screen;
    filter: saturate(1.08);
    mask-image: linear-gradient(90deg, transparent 0%, #000 18%, #000 86%, transparent 100%);
  }

  .flame-canvas,
  .flame-body {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .flame-canvas {
    z-index: 2;
  }

  .flame-body {
    z-index: 1;
    background:
      radial-gradient(ellipse at 50% 86%, rgba(255, 183, 72, 0.42), transparent 34%),
      radial-gradient(ellipse at 54% 58%, rgba(209, 59, 21, 0.2), transparent 46%),
      radial-gradient(ellipse at 56% 36%, rgba(255, 232, 154, 0.1), transparent 34%);
    filter: blur(18px);
    transform-origin: 50% 84%;
    animation: flame-breathe 5.8s ease-in-out infinite alternate;
  }

  @keyframes flame-breathe {
    from {
      transform: scaleX(0.92) scaleY(0.98) translateY(8px);
      opacity: 0.72;
    }

    to {
      transform: scaleX(1.04) scaleY(1.02) translateY(-7px);
      opacity: 0.96;
    }
  }

  .eyebrow,
  .kicker,
  .course-label {
    margin: 0;
    color: #d8aa63;
    font-family: Consolas, 'SFMono-Regular', monospace;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0;
  }

  h1,
  h2,
  h3,
  p {
    overflow-wrap: anywhere;
  }

  h1,
  h2 {
    margin: 0;
    color: #f2dfbd;
    font-family: 'Source Han Serif SC', 'Noto Serif SC', 'Songti SC', 'Times New Roman', serif;
    font-weight: 800;
    letter-spacing: 0;
    line-height: 1.06;
    text-wrap: balance;
  }

  h1 {
    max-width: 880px;
    margin-top: 22px;
    font-size: clamp(5.1rem, 7.2vw, 6.35rem);
  }

  .lead {
    max-width: 720px;
    margin: 28px 0 0;
    color: rgba(233, 224, 208, 0.76);
    font-size: 20px;
    line-height: 1.88;
    text-wrap: pretty;
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 36px;
  }

  .sa-button,
  .course-actions a,
  .profile-links a {
    display: inline-flex;
    min-height: 46px;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(218, 166, 86, 0.42);
    border-radius: 4px;
    padding: 0 18px;
    font-weight: 800;
    text-decoration: none;
    transition:
      transform 160ms ease,
      border-color 160ms ease,
      background 160ms ease,
      color 160ms ease;
  }

  .sa-button.primary {
    border-color: #d8aa63;
    background: #d8aa63;
    color: #15100a;
  }

  .sa-button.quiet,
  .course-actions a,
  .profile-links a {
    color: #f1dfbd;
  }

  .sa-button:hover,
  .course-actions a:hover,
  .profile-links a:hover {
    transform: translateY(-1px);
    border-color: rgba(241, 223, 189, 0.72);
    background: rgba(241, 223, 189, 0.08);
  }

  .sa-button:active,
  .course-actions a:active,
  .profile-links a:active {
    transform: translateY(0);
  }

  .sa-button:focus-visible,
  .course-actions a:focus-visible,
  .profile-links a:focus-visible {
    outline: 2px solid #f1c36f;
    outline-offset: 3px;
  }

  .creed-list h3,
  .course-panel h3 {
    margin: 0;
    color: #f2dfbd;
    font-size: 22px;
    line-height: 1.24;
  }

  .manifesto-body p,
  .section-copy p,
  .course-main p,
  .course-facts dd,
  .creed-list p,
  .sa-footer p {
    color: rgba(233, 224, 208, 0.72);
    line-height: 1.78;
  }

  .manifesto,
  .courses,
  .profile {
    border-top: 1px solid rgba(218, 166, 86, 0.18);
    padding: 92px 0;
  }

  .manifesto {
    display: grid;
    grid-template-columns: minmax(0, 0.58fr) minmax(0, 0.78fr);
    gap: 70px;
  }

  .section-copy h2 {
    margin-top: 14px;
    max-width: 760px;
    font-size: 56px;
  }

  .section-copy.wide {
    max-width: 860px;
  }

  .section-copy > p:not(.kicker) {
    max-width: 720px;
    margin: 22px 0 0;
    font-size: 18px;
  }

  .manifesto-body > p {
    margin: 0;
    font-size: 20px;
  }

  .creed-list {
    display: grid;
    gap: 1px;
    margin-top: 34px;
    border: 1px solid rgba(218, 166, 86, 0.16);
    background: rgba(218, 166, 86, 0.16);
  }

  .creed-list article {
    padding: 22px;
    background: rgba(14, 13, 11, 0.94);
  }

  .creed-list p {
    margin: 8px 0 0;
  }

  .course-panel {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(310px, 0.44fr);
    gap: 40px;
    margin-top: 42px;
    border: 1px solid rgba(218, 166, 86, 0.28);
    border-radius: 8px;
    padding: 34px;
    background: linear-gradient(135deg, rgba(216, 170, 99, 0.12), transparent 42%), rgba(13, 12, 10, 0.92);
    box-shadow: 0 34px 84px rgba(0, 0, 0, 0.28);
  }

  .course-panel h3 {
    margin-top: 14px;
    font-size: 36px;
  }

  .course-main > p:not(.course-label) {
    max-width: 720px;
    margin: 18px 0 0;
    font-size: 17px;
  }

  .course-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 28px;
  }

  .course-actions a {
    min-height: 40px;
    padding: 0 13px;
    font-size: 14px;
  }

  .course-facts {
    display: grid;
    gap: 0;
    margin: 0;
    border-top: 1px solid rgba(218, 166, 86, 0.18);
  }

  .course-facts div {
    display: grid;
    grid-template-columns: 76px 1fr;
    gap: 18px;
    border-bottom: 1px solid rgba(218, 166, 86, 0.18);
    padding: 17px 0;
  }

  .course-facts dt {
    color: #d8aa63;
    font-weight: 850;
  }

  .course-facts dd {
    margin: 0;
  }

  .profile-links {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 34px;
  }

  .profile-links a {
    min-height: 42px;
    padding: 0 14px;
    background: rgba(241, 223, 189, 0.035);
    font-size: 14px;
  }

  .sa-footer {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 28px;
    align-items: start;
    border-top: 1px solid rgba(218, 166, 86, 0.18);
    padding: 28px 0 40px;
  }

  .sa-footer p {
    max-width: 560px;
    margin: 0;
    font-size: 14px;
  }

  .sa-footer div {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    justify-content: flex-end;
  }

  .sa-footer a {
    display: inline-flex;
    min-height: 36px;
    min-width: 36px;
    align-items: center;
    color: rgba(233, 224, 208, 0.72);
    font-size: 14px;
    text-decoration: none;
  }

  .sa-footer a:hover {
    color: #f2dfbd;
  }

  @media (max-width: 1040px) {
    .hero,
    .manifesto,
    .course-panel,
    .sa-footer {
      grid-template-columns: 1fr;
    }

    .hero {
      min-height: auto;
    }

    .flame-field {
      right: -18vw;
      bottom: auto;
      top: 22px;
      width: min(92vw, 620px);
      height: 520px;
      opacity: 0.48;
    }

    .sa-footer div {
      justify-content: flex-start;
    }
  }

  @media (max-width: 720px) {
    .sa-nav,
    main,
    .sa-footer {
      width: min(100% - 28px, 1240px);
    }

    .sa-nav {
      position: static;
      flex-direction: column;
      align-items: flex-start;
      padding: 14px 0;
    }

    .sa-nav nav {
      width: 100%;
      flex-wrap: wrap;
    }

    .hero {
      gap: 42px;
      padding: 58px 0 52px;
    }

    .flame-field {
      right: -36vw;
      top: 18px;
      width: 124vw;
      height: 440px;
      min-height: 360px;
      opacity: 0.34;
    }

    h1 {
      font-size: 3.65rem;
    }

    .lead {
      font-size: 17px;
    }

    .section-copy h2 {
      font-size: 2.6rem;
    }

    .manifesto,
    .courses,
    .profile {
      padding: 60px 0;
    }

    .actions,
    .course-actions,
    .profile-links {
      display: grid;
      grid-template-columns: 1fr;
    }

    .course-facts div {
      grid-template-columns: 1fr;
      gap: 10px;
    }

    .course-panel {
      padding: 22px;
    }

    .course-panel h3 {
      font-size: 2rem;
    }
  }

  @media (max-width: 420px) {
    h1 {
      font-size: 3.05rem;
    }

    .section-copy h2 {
      font-size: 2.15rem;
    }

    .brand-name {
      font-size: 18px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .flame-body {
      animation: none;
    }
  }

  /* writing preview */
  .writing-preview {
    border-top: 1px solid rgba(218, 166, 86, 0.18);
    padding: 92px 0;
  }

  .writing-list {
    display: grid;
    gap: 0;
    margin-top: 42px;
  }

  .writing-card {
    display: block;
    padding: 28px 0;
    border-bottom: 1px solid rgba(218, 166, 86, 0.12);
    text-decoration: none;
    transition: background 160ms ease;
  }

  .writing-card:hover {
    background: rgba(218, 166, 86, 0.03);
  }

  .writing-meta {
    display: flex;
    gap: 14px;
    margin-bottom: 8px;
  }

  .writing-meta time,
  .writing-meta span {
    color: rgba(168, 177, 176, 0.6);
    font-family: Consolas, 'SFMono-Regular', monospace;
    font-size: 13px;
  }

  .writing-card h3 {
    margin: 0;
    color: #f2dfbd;
    font-family: 'Source Han Serif SC', 'Noto Serif SC', 'Songti SC', serif;
    font-size: 24px;
    font-weight: 800;
    line-height: 1.3;
  }

  .writing-card p {
    margin: 8px 0 0;
    color: rgba(233, 224, 208, 0.6);
    font-size: 15px;
    line-height: 1.6;
  }

  .writing-more {
    margin-top: 28px;
  }

  .writing-more a {
    display: inline-flex;
    min-height: 36px;
    align-items: center;
    color: #d8aa63;
    font-size: 15px;
    font-weight: 800;
    text-decoration: none;
  }

  .writing-more a:hover {
    color: #ffe4aa;
  }

  /* homepage redesign override */
  .sa-shell {
    background:
      radial-gradient(circle at 78% 30%, rgba(207, 91, 34, 0.24), transparent 34rem),
      radial-gradient(circle at 16% 78%, rgba(94, 129, 124, 0.16), transparent 28rem),
      linear-gradient(180deg, #090807 0%, #11100d 48%, #090908 100%);
  }

  .sa-shell::before {
    content: '';
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.28;
    background-image:
      linear-gradient(rgba(242, 223, 189, 0.055) 1px, transparent 1px),
      linear-gradient(90deg, rgba(242, 223, 189, 0.045) 1px, transparent 1px);
    background-size: 88px 88px;
    mask-image: radial-gradient(circle at 50% 20%, #000 0 34%, transparent 72%);
  }

  .sa-nav,
  main,
  .sa-footer {
    position: relative;
    z-index: 1;
    width: min(100% - 56px, 1320px);
  }

  .sa-nav {
    min-height: 82px;
    border-bottom-color: rgba(242, 223, 189, 0.14);
    background: rgba(9, 8, 7, 0.68);
  }

  .brand-mark {
    border-color: rgba(220, 154, 80, 0.52);
    background: rgba(246, 204, 128, 0.08);
  }

  .brand-name {
    font-size: 18px;
  }

  .sa-nav nav {
    gap: 4px;
  }

  .sa-nav nav a {
    color: rgba(233, 224, 208, 0.66);
    font-weight: 760;
  }

  .hero {
    grid-template-columns: minmax(0, 0.96fr) minmax(230px, 0.3fr);
    gap: clamp(32px, 5vw, 76px);
    min-height: min(840px, calc(100dvh - 82px));
    padding: clamp(68px, 10vh, 112px) 0 clamp(78px, 12vh, 126px);
  }

  .flame-field {
    right: max(-18vw, -260px);
    bottom: -18px;
    width: min(64vw, 860px);
    height: min(80vh, 780px);
    opacity: 0.72;
    filter: saturate(0.96) contrast(1.04);
    mask-image: linear-gradient(90deg, transparent 0%, #000 24%, #000 82%, transparent 100%);
  }

  .eyebrow,
  .kicker,
  .course-label {
    color: #7da8a3;
    font-size: 12px;
    letter-spacing: 0;
  }

  h1 {
    max-width: 920px;
    margin-top: 18px;
    font-size: clamp(5.4rem, 15vw, 13rem);
    line-height: 0.9;
  }

  .lead {
    max-width: 560px;
    margin-top: 30px;
    color: rgba(239, 230, 213, 0.78);
    font-family: 'Source Han Serif SC', 'Noto Serif SC', 'Songti SC', serif;
    font-size: clamp(1.35rem, 2.2vw, 2rem);
    line-height: 1.6;
  }

  .actions {
    gap: 10px;
    margin-top: 34px;
  }

  .sa-button,
  .course-actions a,
  .profile-links a {
    border-radius: 2px;
    min-height: 44px;
    border-color: rgba(242, 223, 189, 0.22);
    background: rgba(242, 223, 189, 0.035);
    color: #f0dfbf;
  }

  .sa-button.primary {
    border-color: #f0c978;
    background: #f0c978;
    color: #15100a;
  }

  .sa-button:hover,
  .course-actions a:hover,
  .profile-links a:hover {
    transform: translateY(-2px);
    border-color: rgba(125, 168, 163, 0.72);
    background: rgba(125, 168, 163, 0.1);
  }

  .hero-aside {
    position: relative;
    z-index: 2;
    align-self: end;
    padding: 0 0 10px 22px;
    border-left: 1px solid rgba(125, 168, 163, 0.36);
  }

  .hero-aside p {
    max-width: 280px;
    margin: 0;
    color: rgba(233, 224, 208, 0.76);
    font-family: 'Source Han Serif SC', 'Noto Serif SC', 'Songti SC', serif;
    font-size: 22px;
    line-height: 1.46;
  }

  .hero-aside div {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 22px;
  }

  .hero-aside span {
    border: 1px solid rgba(242, 223, 189, 0.16);
    border-radius: 2px;
    padding: 6px 9px;
    color: rgba(230, 238, 232, 0.68);
    font-size: 12px;
  }

  .manifesto,
  .writing-preview,
  .courses,
  .profile {
    border-top-color: rgba(242, 223, 189, 0.12);
    padding: clamp(72px, 10vw, 116px) 0;
  }

  .manifesto {
    grid-template-columns: minmax(0, 0.46fr) minmax(0, 0.74fr);
  }

  .section-copy h2 {
    max-width: 780px;
    font-size: clamp(2.5rem, 6vw, 5.6rem);
    line-height: 0.98;
  }

  .section-copy > p:not(.kicker) {
    color: rgba(233, 224, 208, 0.62);
  }

  .manifesto-body > p {
    max-width: 620px;
    color: rgba(242, 223, 189, 0.84);
    font-family: 'Source Han Serif SC', 'Noto Serif SC', 'Songti SC', serif;
    font-size: clamp(1.35rem, 2.5vw, 2.3rem);
    line-height: 1.42;
  }

  .creed-list {
    border: 0;
    background: transparent;
    gap: 0;
    margin-top: 40px;
  }

  .creed-list article {
    display: grid;
    grid-template-columns: minmax(130px, 0.34fr) 1fr;
    gap: 26px;
    border-top: 1px solid rgba(242, 223, 189, 0.12);
    padding: 18px 0;
    background: transparent;
  }

  .creed-list h3 {
    color: #f2dfbd;
    font-size: 18px;
  }

  .creed-list p {
    margin: 0;
    color: rgba(233, 224, 208, 0.62);
  }

  .writing-list {
    margin-top: 34px;
  }

  .writing-card {
    display: grid;
    grid-template-columns: minmax(160px, 0.22fr) minmax(0, 1fr);
    gap: 28px;
    padding: 26px 0;
    border-bottom-color: rgba(242, 223, 189, 0.1);
  }

  .writing-card:hover {
    background: linear-gradient(90deg, rgba(125, 168, 163, 0.055), transparent);
  }

  .writing-meta {
    display: grid;
    align-content: start;
    gap: 6px;
    margin: 0;
  }

  .writing-card h3 {
    font-size: clamp(1.35rem, 2.6vw, 2.25rem);
  }

  .writing-card p {
    max-width: 760px;
    color: rgba(233, 224, 208, 0.58);
  }

  .course-panel {
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.36fr);
    border: 0;
    border-top: 1px solid rgba(242, 223, 189, 0.14);
    border-radius: 0;
    padding: 34px 0 0;
    background: transparent;
    box-shadow: none;
  }

  .course-panel h3 {
    max-width: 760px;
    font-size: clamp(2rem, 4.6vw, 4.2rem);
    line-height: 1;
  }

  .course-main > p:not(.course-label) {
    max-width: 680px;
  }

  .course-facts {
    border-top-color: rgba(125, 168, 163, 0.24);
  }

  .course-facts div {
    grid-template-columns: 74px 1fr;
    border-bottom-color: rgba(125, 168, 163, 0.2);
  }

  .course-facts dt {
    color: #7da8a3;
  }

  .profile-links {
    max-width: 920px;
  }

  .sa-footer {
    border-top-color: rgba(242, 223, 189, 0.12);
  }

  @media (max-width: 1040px) {
    .hero {
      grid-template-columns: 1fr;
      min-height: auto;
    }

    .hero-aside {
      max-width: 640px;
      align-self: start;
    }

    .flame-field {
      right: -28vw;
      width: 96vw;
      opacity: 0.38;
    }
  }

  @media (max-width: 720px) {
    .sa-nav,
    main,
    .sa-footer {
      width: min(100% - 28px, 1320px);
    }

    .hero {
      padding: 58px 0 64px;
    }

    h1 {
      font-size: clamp(4.35rem, 23vw, 7.2rem);
    }

    .lead {
      font-size: 1.25rem;
    }

    .hero-aside {
      padding-left: 16px;
    }

    .hero-aside p {
      font-size: 19px;
    }

    .manifesto {
      grid-template-columns: 1fr;
      gap: 34px;
    }

    .creed-list article,
    .writing-card,
    .course-panel {
      grid-template-columns: 1fr;
    }

    .actions,
    .course-actions,
    .profile-links {
      display: flex;
    }
  }

  @media (max-width: 430px) {
    .actions,
    .course-actions,
    .profile-links {
      display: grid;
      grid-template-columns: 1fr;
    }
  }

  /* final visual pass */
  .hero {
    overflow: hidden;
    border-bottom: 1px solid rgba(242, 223, 189, 0.12);
    min-height: min(720px, calc(100dvh - 82px));
    padding: clamp(48px, 8vh, 82px) 0 clamp(58px, 10vh, 98px);
    background:
      linear-gradient(90deg, rgba(9, 8, 7, 0.98) 0%, rgba(9, 8, 7, 0.9) 48%, rgba(9, 8, 7, 0.52) 100%),
      radial-gradient(circle at 85% 78%, rgba(223, 105, 39, 0.18), transparent 28rem);
  }

  .flame-field {
    right: max(-9vw, -132px);
    bottom: -28px;
    width: min(48vw, 640px);
    height: min(70vh, 640px);
    mix-blend-mode: normal;
    opacity: 0.86;
    mask-image: radial-gradient(ellipse at 58% 64%, #000 0 52%, transparent 78%);
  }

  h1 {
    max-width: none;
    font-size: clamp(4.8rem, 10.5vw, 9.6rem);
    white-space: nowrap;
  }

  .hero-aside {
    margin-bottom: clamp(22px, 6vh, 72px);
  }

  @media (max-width: 720px) {
    .hero {
      background: linear-gradient(180deg, rgba(9, 8, 7, 0.96), rgba(9, 8, 7, 0.74));
    }

    .flame-field {
      right: -44vw;
      width: 112vw;
      opacity: 0.34;
    }

    h1 {
      white-space: normal;
    }
  }
</style>
