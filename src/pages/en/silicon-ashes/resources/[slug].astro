---
import ResourceTemplatePage from '../../../../components/ResourceTemplatePage.astro';
import { resourceTemplates } from '../../../../data/siliconAshesResources';
import SiliconEmbersLayout from '../../../../layouts/SiliconEmbersLayout.astro';

export function getStaticPaths() {
  return resourceTemplates.map((template) => ({
    params: { slug: template.slug },
    props: { template },
  }));
}

const { template } = Astro.props;
const data = template.en;
const canonicalPath = `/en/silicon-ashes/resources/${template.slug}/`;
const alternatePath = `/silicon-ashes/resources/${template.slug}/`;
---

<SiliconEmbersLayout
  lang="en"
  title={`${data.title} | Silicon Ashes Resources`}
  description={data.summary}
  canonicalPath={canonicalPath}
  alternatePath={alternatePath}
>
  <ResourceTemplatePage lang="en" template={template} />
</SiliconEmbersLayout>
