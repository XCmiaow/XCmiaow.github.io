---
import { getCollection, render } from 'astro:content';
import SiliconEmbersArticlePage from '../../../../components/silicon-embers/SiliconEmbersArticlePage.astro';
import { siliconEmbersArticleAlternates } from '../../../../data/siliconEmbersHome';
import SiliconEmbersLayout from '../../../../layouts/SiliconEmbersLayout.astro';

export async function getStaticPaths() {
  const posts = await getCollection('blog', ({ data }) => data.lang === 'en' && !data.draft);
  return posts.map((post) => ({
    params: { slug: post.id },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
const allPosts = (await getCollection('blog', ({ data }) => data.lang === 'en' && !data.draft)).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf(),
);
const canonicalPath = `/en/silicon-ashes/writing/${post.id}/`;
const alternateId = siliconEmbersArticleAlternates[post.id];
const alternatePath = alternateId ? `/silicon-ashes/writing/${alternateId}/` : undefined;
---

<SiliconEmbersLayout
  lang="en"
  title={`${post.data.title} | Silicon Ashes`}
  description={post.data.description ?? post.data.title}
  canonicalPath={canonicalPath}
  alternatePath={alternatePath}
>
  <SiliconEmbersArticlePage lang="en" post={post} allPosts={allPosts} Content={Content} />
</SiliconEmbersLayout>
